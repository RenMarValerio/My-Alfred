services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: karen
      POSTGRES_PASSWORD: karen
      POSTGRES_DB: karen
    ports:
      - '5432:5432'
    volumes:
      - karen-db-data:/var/lib/postgresql/data

volumes:
  karen-db-data:
